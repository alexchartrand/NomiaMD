[project]
name = "nomiamd-backend"
version = "0.1.0"
description = "Extracts RAMQ billing codes (and future outputs) from clinical transcripts"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "openai>=1.40",
    "pydantic>=2.9",
    "sqlalchemy>=2.0",
    "python-dotenv>=1.0",
    "llama-index-core>=0.12",
    "llama-index-embeddings-mistralai>=0.3",
    "llama-index-llms-mistralai>=0.10",
    "llama-index-vector-stores-lancedb>=0.5",
    "llama-index-retrievers-bm25>=0.7",
    "pandas>=2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "httpx>=0.27",
]

[tool.setuptools.packages.find]
include = ["app*"]
